# handle JCEF unavailable
* detect the reason . if it's JDK issue that doesn't contains JCEF, tell users to change setup
* or provide a link to show in the external browser

# test `<apply>`, `<include>` works for zul preview

# test zul syntax according to zuml reference
For each group of syntax, at least choose three cases to verify. Does it work in zul preview?


# test within springboot jar packaging project