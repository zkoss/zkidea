# E4 — Verifier verdict (round 1, Opus, fresh context)

Verdict date: 2026-07-07. All evidence below was reproduced by the verifier, not taken from the maker's report.

## Per-check results

| Check | Gate | Verdict | Evidence (verifier-produced) |
|-------|------|---------|------------------------------|
| V1 | E4-G1 build green | **PASS** | `withjdk.sh 17 ./gradlew build -x buildSearchableOptions` → `BUILD SUCCESSFUL in 13s`, wrapper `BUILD_EXIT=0`. Root `build/test-results/test/*.xml` tally: **tests=300 failures=0 errors=0**. Launcher `zk-preview-launcher/build/test-results/test/*.xml` tally: **tests=36 failures=0 errors=0**. Root build task graph from MY invocation includes `> Task :zk-preview-launcher:test`, `:check`, `:build` (UP-TO-DATE — inputs unchanged since maker's run, so cached results are current) and root `> Task :test` executed. Confirms the launcher suite runs as part of the ROOT `build` invocation (W2 claim). |
| V2 | Bundling intact | **PASS** | `.sandbox/plugins/zkidea/lib/zk-preview-launcher.jar` present (465063 bytes). `build/distributions/zkidea-0.7.2.zip` contains `zkidea/lib/zk-preview-launcher.jar`. `build.gradle:62-66` wires `prepareSandbox` with `dependsOn ':zk-preview-launcher:jar'` copying the jar `into "${project.name}/lib"`. |
| V3 | E4-G2 docs accurate | **PASS** | `doc/feature_overview.md` has `## 10. ZUL Preview` section. `README.md` has `### ZUL Preview` Features entry. Spot-checked 6+ claims against code, all accurate: (1) CLI `--classpath/--webapp/--port` + stdout `PREVIEW_PORT=<n>` matches `Main.java:20-37`; (2) `RenderPhase` enum = exactly PARSE/COMPOSE/UNKNOWN/CLASSPATH/RESOURCE (CLASSPATH/RESOURCE reserved) matches `RenderPhase.java`; (3) all named launcher classes exist (Main, PreviewHttpServer, RenderEngineFactory, RenderEngine, VariantDetector, Javax/JakartaRenderEngine, ScopedZkClassLoader, IsolatedRuntime, ErrorMapper); (4) `PreviewUiFactory extends SimpleUiFactory` overrides `newComposer` + `getPageDefinition` in the `hooks` sourceSet matches `PreviewUiFactory.java:48,69,77,85`; (5) "one server per (docroot, classpath-signature)" matches `ZulPreviewServerService.java:84` key = `target.docroot + "#" + target.classpathSignature`; (6) provider `accept()` PSI-free `.zul` check + `getPolicy()`=`HIDE_DEFAULT_EDITOR` matches `ZulPreviewFileEditorProvider.java:34,46-47`. ErrorMapper COMPOSE-coverage prose (CNFE + BeanShell + UiException) matches `ErrorMapper.map()`. No factually wrong statement found. |
| V4 | W4 addon prefixes (TDD) | **PASS** | `ZkClasspathFilter.java:25-27` `ZK_ARTIFACT_PREFIXES` now includes `zkcharts-`, `zkpivot-`, `keikai-` (used only by `isZkJar`/`filterZkJars` = R7 gate). Test `recognizesAddonOnlyJarsAsZkJarsForTheR7Gate()` exists (`ZkClasspathFilterTest.java:41-49`) and passes (part of the 300 green root tests). `filterLibraryJars` (`ZkClasspathFilter.java:70-79`) is prefix-agnostic (`file.isFile()`), structurally independent of the prefix set — change is confined to the R7 path. |
| V5 | W5 ErrorMapper (TDD + no regression) | **PASS** | Classification is STRUCTURAL, not message-parsing: `isUiException()` (`ErrorMapper.java:94-103`) walks each throwable's own Java class hierarchy via `getSuperclass()` looking for FQCN `org.zkoss.zk.ui.UiException`. New branch (`ErrorMapper.java:46-52`) sits AFTER the missing-class and `looksLikeParseError` checks, so fixture (f)/missing-class paths are unaffected by ordering. `HierarchyFailureTest.java` reproduces "Unsupported parent for row" (`<row>` under `<window>`, fixture `unsupported-parent.zul`) and asserts `RenderPhase.COMPOSE` — drives the real `RenderEngineFactory.create(...).renderZul()` in both servlet variants, passing (in the 36 green). No-regression: `StructuredFailureTest.java:38-48` (fixture (f) zscript missing-class = the in-repo "broken.zul-style" COMPOSE-with-line contract) asserts non-null phase, missing-FQCN message, and non-null line — passing. Live spot-check skipped (optional/"only if cheap") — the passing HierarchyFailureTest is itself a live render, not a mock, so it supersedes a manual curl. |
| V6 | W3 hook stub sanity | **PASS** | `tasks/zul-preview/stage2-hook.md` exists; its JSON schema (`status`, `error.{phase,message,zulFile,line,column}`, FAILURE envelope) matches `ErrorMapper`/`RenderError`/`RenderResult.toJson()` output and the maker's captured JSON. Pointer comment present at the failure-response location `PreviewHttpServer.java:69-71` (the `!isSuccess()` → `r.toJson()` HTTP-500 branch). Premise verified: grep of `src/main/java/org/zkoss/zkidea/preview/` finds NO code parsing the launcher response JSON — only `ManagedPreviewServer` "failure/failed" hits, which concern process-start `Throwable`s, not HTTP-response parsing. Documentation-over-listener choice is justified. |
| V7 | E4-G3 + scope hygiene | **PASS** | `withjdk.sh 17 ./gradlew verifyPlugin` → `BUILD SUCCESSFUL in 660ms`, zero problems. `plugin.xml` diff = exactly the 2 E3 lines (`fileEditorProvider` + `projectService`), NO new E4 edit. `git log --oneline` unchanged (HEAD still `35348ef` = session start) — no commits made. Tracked-modified files (README.md, build.gradle, doc/feature_overview.md, settings.gradle, plugin.xml, scope-var-completion.zul) are all either E4-expected (README, feature_overview) or pre-existing E1–E3 modifications present in the session-start git status. E4's new files live inside already-untracked dirs (zk-preview-launcher/, tasks/) so they don't surface as separate stray entries. No stray files outside E4 scope. |

## Blocking defects

None.

## Non-blocking suggestions

- W2's launcher-coverage relies on Gradle's implicit "unqualified task name matches all subprojects" behavior with no explicit `check.dependsOn ':zk-preview-launcher:check'`. Works today; a future build restructure could silently drop launcher coverage. A one-line comment or defensive `dependsOn` would harden it. (Maker already flagged this honestly.)
- `looksLikeParseError` vs. the new `isUiException` ordering has no PARSE-phase fixture exercising it (pre-existing gap, not introduced this round).
- `stage2-hook.md` schema has no automated test locking it to the code; prose could drift. `StructuredFailureTest`/`HierarchyFailureTest` cover the underlying JSON shape indirectly.

## Ready for Phase 6: YES
