# Stamped by processResources from build.gradle's project.version. Read back by
# PreviewIssueReporter.pluginVersion(): asking the platform for our own plugin descriptor is an
# internal API the Marketplace rejects, and the version is a build-time constant anyway.
version=${pluginVersion}
