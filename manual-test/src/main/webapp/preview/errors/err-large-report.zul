<zk>
    <window title="large layout — clipboard-fallback fixture" border="normal">
        <!-- Intentionally LARGE. The render-error report (this .zul source + the
             environment + the full stack trace) exceeds the safe ~8 KB GitHub URL
             length, so the error page must switch from a pre-filled "Report on
             GitHub" link to the copy-to-clipboard hand-off (a button that copies the
             complete report to the clipboard and opens a title-only new issue).
             See tasks/error-reporting/CLIPBOARD-FALLBACK.md. -->
        <vbox>
            <label value="Row 001 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 002 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 003 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 004 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 005 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 006 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 007 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 008 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 009 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 010 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 011 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 012 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 013 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 014 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 015 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 016 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 017 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 018 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 019 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 020 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 021 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 022 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 023 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 024 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 025 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 026 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 027 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 028 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 029 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 030 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 031 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 032 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 033 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 034 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 035 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 036 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 037 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 038 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 039 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 040 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 041 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 042 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 043 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 044 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 045 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 046 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 047 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 048 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 049 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 050 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 051 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 052 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 053 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 054 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 055 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 056 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 057 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 058 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 059 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 060 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 061 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 062 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 063 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 064 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 065 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 066 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 067 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 068 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 069 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 070 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 071 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 072 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 073 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 074 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 075 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 076 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 077 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 078 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 079 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 080 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 081 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 082 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 083 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 084 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 085 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 086 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 087 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 088 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 089 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 090 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 091 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 092 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 093 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 094 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 095 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 096 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 097 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 098 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 099 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 100 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 101 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 102 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 103 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 104 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 105 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 106 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 107 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 108 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 109 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 110 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 111 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 112 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 113 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 114 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 115 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 116 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 117 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 118 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 119 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 120 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 121 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 122 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 123 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 124 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 125 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 126 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 127 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 128 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 129 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 130 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 131 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 132 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 133 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 134 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 135 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 136 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 137 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 138 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 139 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 140 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 141 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 142 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 143 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 144 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 145 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 146 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 147 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 148 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 149 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 150 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 151 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 152 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 153 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 154 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 155 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 156 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 157 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 158 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 159 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 160 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 161 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 162 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 163 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 164 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 165 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 166 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 167 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 168 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 169 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
            <label value="Row 170 — placeholder content that pads the source so the render-error report is large enough to trigger the clipboard fallback"/>
        </vbox>
        <!-- ...and it also fails to render: this class is not on the classpath, so
             BeanShell fails at compose time -> a COMPOSE error with a real trace. -->
        <zscript>
            com.example.reporting.HugeReportBuilder b = new com.example.reporting.HugeReportBuilder();
        </zscript>
        <label value="never reached"/>
    </window>
</zk>
